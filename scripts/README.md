# Scripts

Scripts are grouped by purpose:

- `runtime/` - helpers for running or accepting local app actions.
- `maintenance/` - database cleanup, inspection, and repair utilities.
- `import/` - SOP upload/import helpers.
- `verification/` - manual regression and behavior checks.
- `deployment/` - server/deployment helpers.

Run scripts from the project root unless the script says otherwise.
