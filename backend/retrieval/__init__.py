# Retrieval module initialization
"""
Compatibility package.

The chatbot code is being consolidated under `backend/chatbot/`.
Keep this package so existing imports (`retrieval.*`) continue to work.
"""
